fun funktio(syote) {
/* 
This is multiline comment and this will be revoved by the tokeziner.

print_str("hello world");
*/
	print_str(syote);

	var i1: Int = str_to_int(syote);

	print_int(i1);

	if pow2(i1) < pow2(i1 * 2) then {
		i1 = i1 / 64;
		var new_i1 = i1 * i1 * i1 * i1;
		print_int(new_i1);
		var s3 = "and back to integer type ";
		var s2 = int_to_str(new_i1);
		print_str2(s3);
		print_str(s2);

		var final = str_cat(s3, s2);
		print_str(final);
		var return_o: Int = true;
	}

}

fun sqrt(d): Int {

	var i = 1;
	
	while true do {
		// we don't want to live forever in the loop ;)
		if i > 1000 then { return 0; }
		if i * i == d then {
			return i;
		}
		i = i + 1;
	}
}

var input = str_to_int("81");
print_int(sqrt(input));
